{
    "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentTemplate.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
        "afd_profile_id": {
            "type": "string",
            "metadata": {
                "description": "The ID of the Azure Front Door profile"
            }
        },
        "afd_endpoint_id": {
            "type": "string",
            "metadata": {
                "description": "The ID of the Azure Front Door endpoint"
            }
        },
        "associationType": {
            "type": "string",
            "defaultValue": "All",
            "metadata": {
                "description": "The type of association",
                "allowedValues": [
                    "All",
                    "Individually"
                ]
            }
        },
        "all_ruleset_id": {
            "type": "string",
            "defaultValue": "---",
            "metadata": {
                "description": "The ID of the ruleset to apply to all routes if associationType is All"
            }
        },
        "wwwposteit-modify": {
            "type": "bool",
            "defaultValue": false,
            "metadata": {
                "description": "True to modify the route, false to ignore it"
            }
        },
        "wwwposteit-ruleset": {
            "type": "string",
            "defaultValue": "---",
            "metadata": {
                "description": "The ruleset to apply to the route"
            }
        },
        "postepayposteit-modify": {
            "type": "bool",
            "defaultValue": false,
            "metadata": {
                "description": "True to modify the route, false to ignore it"
            }
        },
        "postepayposteit-ruleset": {
            "type": "string",
            "defaultValue": "---",
            "metadata": {
                "description": "The ruleset to apply to the route"
            }
        }
    },
    "variables": {
        "afd_profile_name": "[last(split(parameters('afd_profile_id'), '/'))]",
        "afd_endpoint_name": "[last(split(parameters('afd_endpoint_id'), '/'))]",
        "wwwposteit_route_name": "routing-www-poste-it",
        "wwwposteit_domain_id": "[concat(parameters('afd_profile_id'), '/customDomains/domain-www-poste-it')]",
        "wwwposteit_origingroup_id": "[concat(parameters('afd_profile_id'), '/originGroups/origingroup-www-poste-it-AZ')]",
        "postepayposteit_route_name": "routing-postepay-poste-it",
        "postepayposteit_domain_id": "[concat(parameters('afd_profile_id'), '/customDomains/domain-postepay-poste-it')]",
        "postepayposteit_origingroup_id": "[concat(parameters('afd_profile_id'), '/originGroups/origingroup-postepay-poste-it-AZ')]"
    },
    "resources": [
        {
            "type": "Microsoft.Cdn/profiles/afdendpoints",
            "apiVersion": "2024-02-01",
            "name": "[concat(variables('afd_profile_name'), '/', variables('afd_endpoint_name'))]",
            "location": "global"
        },
        {
            "type": "Microsoft.Cdn/profiles/afdendpoints/routes",
            "apiVersion": "2024-02-01",
            "name": "[concat(variables('afd_profile_name'), '/', variables('afd_endpoint_name'), '/', variables('wwwposteit_route_name'))]",
            "dependsOn": [
                "[parameters('afd_endpoint_id')]"
            ],
            "condition": "[or(equals(parameters('associationType'), 'All'), parameters('wwwposteit-modify'))]",
            "properties": {
                "customDomains": [
                    {
                        "id": "[variables('wwwposteit_domain_id')]"
                    }
                ],
                "originGroup": {
                    "id": "[variables('wwwposteit_origingroup_id')]"
                },
                "ruleSets": "[if(equals(parameters('associationType'), 'All'), if(equals(parameters('all_ruleset_id'), '---'), json('[]'), array(createObject('id', parameters('all_ruleset_id')))), if(equals(parameters('wwwposteit-ruleset'), '---'), json('[]'), array(createObject('id', parameters('wwwposteit-ruleset')))))]"
            }
        },
        {
            "type": "Microsoft.Cdn/profiles/afdendpoints/routes",
            "apiVersion": "2024-02-01",
            "name": "[concat(variables('afd_profile_name'), '/', variables('afd_endpoint_name'), '/', variables('postepayposteit_route_name'))]",
            "dependsOn": [
                "[parameters('afd_endpoint_id')]"
            ],
            "condition": "[or(equals(parameters('associationType'), 'All'), parameters('postepayposteit-modify'))]",
            "properties": {
                "customDomains": [
                    {
                        "id": "[variables('postepayposteit_domain_id')]"
                    }
                ],
                "originGroup": {
                    "id": "[variables('postepayposteit_origingroup_id')]"
                },
                "ruleSets": "[if(equals(parameters('associationType'), 'All'), if(equals(parameters('all_ruleset_id'), '---'), json('[]'), array(createObject('id', parameters('all_ruleset_id')))), if(equals(parameters('postepayposteit-ruleset'), '---'), json('[]'), array(createObject('id', parameters('postepayposteit-ruleset')))))]"
            }
        }
    ]
}